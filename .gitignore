# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Secrets — never commit
.env
.env.*
!.env.example

# Lighthouse report dumps (regenerate any time, ~1MB each)
desktop.json
mobile.json

# Editor / tooling
.vscode/
.idea/
node_modules/
